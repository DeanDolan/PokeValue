<%# Main display values used across the product page, buttons and forms %>
<% product_logged_in = defined?(user_signed_in?) && user_signed_in? %>
<% product_admin = defined?(admin_signed_in?) && admin_signed_in? %>
<% display_product_name = @product_name.presence || @product_type_name %>
<% display_product_img_url = @product_img_url %>
<% display_product_value = @product_value.present? ? number_to_currency(@product_value, unit: "€") : "€0.00" %>

<%# Forecast metadata sent to the forecast Stimulus controller %>
<% derived_variant = params[:product_variant].presence || display_product_name.to_s[/\(([^)]+)\)/, 1].to_s.strip %>
<% derived_origin = params[:origin].presence || (display_product_name.to_s.downcase.include?("pokemon center") ? "Pokemon Center" : "") %>

<%# Product information cards shown beside the product image %>
<% product_cards = [
  ["Set", @set_name],
  ["Era", @era],
  ["Release Date", @release_date],
  ["Product Type", @product_type_name],
  ["Estimated Value", display_product_value],
  ["Listings", @marketplace_listing_quantity]
] %>

<%# Forecast buttons and milestone cards %>
<% forecast_horizons = [["6m", "6m"], ["1y", "1y"], ["3y", "3y"], ["5y", "5y"]] %>
<% forecast_targets = [["6m", "m6"], ["1y", "m1y"], ["3y", "m3y"], ["5y", "m5y"]] %>

<div class="container py-4">
  <div class="mb-3">
    <%= link_to "← Back to set", set_path(slug: @set_slug), class: "btn btn-link p-0" %>
  </div>

  <div class="row g-4 align-items-stretch">
    <div class="col-12 col-lg-5">
      <div class="card h-100">
        <div class="card-body d-flex align-items-center justify-content-center">
          <%= image_tag display_product_img_url, alt: display_product_name, class: "img-fluid", style: "max-height:320px;object-fit:contain;" %>
        </div>
      </div>
    </div>

    <div class="col-12 col-lg-7">
      <div class="text-center mb-3">
        <%= image_tag @set_logo_url, alt: @set_name, class: "mb-2", style: "max-height:60px;object-fit:contain;" %>

        <h3 class="mb-0"><%= display_product_name %></h3>

        <div class="text-muted small">
          <%= [@era, @set_name, @product_type_name].compact.join(" · ") %>
        </div>

        <%# Shows whether the current user already owns or watches this product %>
        <% if @holdings_qty.to_i.positive? %>
          <div class="mt-2">
            <span class="badge text-bg-danger"><%= "#{@holdings_qty} in holdings" %></span>
          </div>
        <% end %>

        <% if @is_watchlisted %>
          <div class="mt-2">
            <span class="badge text-bg-success">On your watchlist</span>
          </div>
        <% end %>
      </div>

      <%# Product information cards %>
      <div class="row g-3 mb-4">
        <% product_cards.each do |label, value| %>
          <div class="col-6 col-md-4">
            <div class="card h-100">
              <div class="card-body text-center py-3">
                <div class="small text-muted"><%= label %></div>
                <div class="fw-semibold"><%= value %></div>

                <%# Admin-only product value update form %>
                <% if label == "Estimated Value" && product_admin %>
                  <div class="mt-2">
                    <%= form_with url: admin_product_value_path(sku: @admin_value_sku), method: :patch, local: true do %>
                      <%= hidden_field_tag :return_to, request.fullpath %>
                      <%= hidden_field_tag :name, display_product_name %>
                      <%= hidden_field_tag :set_name, @set_name %>
                      <%= hidden_field_tag :era, @era %>
                      <%= hidden_field_tag :product_type_name, @product_type_name %>
                      <%= hidden_field_tag :image_url, display_product_img_url %>

                      <div class="input-group input-group-sm">
                        <%= number_field_tag :value, @product_value, step: "0.01", min: 0, class: "form-control", required: true %>
                        <button class="btn btn-outline-primary" type="submit">Update</button>
                      </div>
                    <% end %>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>

      <%# Product action buttons %>
      <div class="d-flex flex-wrap gap-2 justify-content-center">
        <% if product_logged_in %>
          <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#addHoldingModal">
            Add to Portfolio
          </button>
        <% else %>
          <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#authModal" data-auth-title="Log in to add to portfolio">
            Add to Portfolio
          </button>
        <% end %>

        <%# Watchlist button switches between add and remove depending on the saved state %>
        <% if product_logged_in %>
          <% watchlist_path_for_product = @is_watchlisted ? remove_watchlist_path(sku: @watchlist_sku) : watchlist_path(sku: @watchlist_sku) %>
          <% watchlist_method = @is_watchlisted ? :delete : :post %>
          <% watchlist_label = @is_watchlisted ? "Remove from Watchlist" : "Add to Watchlist" %>
          <% watchlist_class = @is_watchlisted ? "btn btn-outline-danger" : "btn btn-outline-secondary" %>

          <%= button_to watchlist_label,
                        watchlist_path_for_product,
                        method: watchlist_method,
                        form: { class: "d-inline" },
                        params: { return_to: request.fullpath },
                        class: watchlist_class %>
        <% else %>
          <button class="btn btn-outline-secondary" data-bs-toggle="modal" data-bs-target="#authModal" data-auth-title="Log in to add to watchlist">
            Add to Watchlist
          </button>
        <% end %>

        <%# Future Projections opens the forecast modal for logged-in users %>
        <% if product_logged_in %>
          <button
            class="btn btn-outline-primary"
            data-bs-toggle="modal"
            data-bs-target="#forecastModal"
            data-forecast-set-name="<%= @set_name %>"
            data-forecast-product-name="<%= display_product_name %>"
            data-forecast-product-variant="<%= derived_variant %>"
            data-forecast-origin="<%= derived_origin %>">
            Future Projections
          </button>
        <% else %>
          <button class="btn btn-outline-primary" data-bs-toggle="modal" data-bs-target="#authModal" data-auth-title="Log in to see future projections">
            Future Projections
          </button>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%# Future projections modal %>
<% if product_logged_in %>
  <div class="modal fade" id="forecastModal" tabindex="-1" data-controller="forecast" data-forecast-endpoint-value="/forecast">
    <div class="modal-dialog modal-xl modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Future Projections</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>

        <div class="modal-body">
          <div class="alert alert-warning small mb-3">
            <strong>Warning:</strong>
            Future projections are estimates only. They are based on available historical data and trend-based modelling, so they are not guaranteed to be accurate. These projections should not be treated as financial advice or relied on as the only reason to buy, sell, or hold a product. Product values can rise or fall, and PokeValue is not liable for any loss, missed profit, or financial decision made using these projections.
          </div>

          <div class="d-flex gap-2 mb-3">
            <% forecast_horizons.each do |label, horizon| %>
              <button class="btn btn-outline-primary btn-sm" data-forecast-target="hbtn" data-horizon="<%= horizon %>" data-action="click->forecast#changeHorizon">
                <%= label %>
              </button>
            <% end %>

            <div class="ms-auto small text-muted">
              As of: <span data-forecast-target="asof">—</span>
            </div>
          </div>

          <div class="small text-muted mb-2" data-forecast-target="status">Loading…</div>

          <div style="height:360px;">
            <canvas data-forecast-target="canvas"></canvas>
          </div>

          <hr>

          <%# Forecast milestone values %>
          <div class="row g-2 text-center">
            <% forecast_targets.each do |label, target| %>
              <div class="col-6 col-lg-3">
                <div class="card">
                  <div class="card-body">
                    <div class="small text-muted"><%= label %></div>
                    <div class="fw-semibold" data-forecast-target="<%= target %>">—</div>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <%# Add to Portfolio modal %>
  <div class="modal fade" id="addHoldingModal" tabindex="-1" aria-labelledby="addHoldingModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="addHoldingModalLabel">Add to Portfolio</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>

        <div class="modal-body">
          <div class="mb-3 small text-muted text-center">
            <div><strong><%= display_product_name %></strong></div>
            <div><%= @set_name %> · <%= @era %></div>
          </div>

          <%# This form posts the selected product directly into the holdings table %>
          <%= form_with url: holdings_path,
                        scope: :holding,
                        method: :post,
                        local: true,
                        data: { controller: "holding-value", holding_value_base_value: @product_value.to_f } do %>
            <%= hidden_field_tag "holding[set_slug]", @set_slug %>
            <%= hidden_field_tag "holding[type_code]", @current_route_type %>
            <%= hidden_field_tag "holding[image_url]", display_product_img_url %>
            <%= hidden_field_tag "holding[era]", @era %>
            <%= hidden_field_tag "holding[set_name]", @set_name %>
            <%= hidden_field_tag "holding[product_type]", display_product_name %>

            <div class="mb-3">
              <label class="form-label" for="holding_value_input">Estimated value / unit (€)</label>
              <%= number_field_tag "holding[value]",
                                   @product_value,
                                   step: "0.01",
                                   min: 0,
                                   class: "form-control",
                                   id: "holding_value_input",
                                   required: true,
                                   readonly: true,
                                   data: { holding_value_target: "value" } %>
              <div class="form-text" id="holding_condition_note" data-holding-value-target="note"></div>
            </div>

            <div class="mb-3">
              <label class="form-label" for="holding_quantity">Quantity</label>
              <%= number_field_tag "holding[quantity]", 1, min: 1, class: "form-control", id: "holding_quantity", required: true %>
            </div>

            <div class="mb-3">
              <label class="form-label" for="holding_cost_per_unit">Cost per unit (€)</label>
              <%= number_field_tag "holding[cost_per_unit]", @product_value, step: "0.01", min: 0, class: "form-control", id: "holding_cost_per_unit", required: true %>
            </div>

            <div class="mb-3">
              <label class="form-label" for="holding_purchase_date">Purchase date</label>
              <%= date_field_tag "holding[purchase_date]", Date.today, class: "form-control", id: "holding_purchase_date" %>
            </div>

            <div class="mb-3">
              <label class="form-label" for="holding_condition">Condition</label>
              <%= select_tag "holding[condition]",
                             options_for_select(@condition_options),
                             include_blank: "Select condition",
                             class: "form-select",
                             id: "holding_condition",
                             data: { holding_value_target: "condition", action: "change->holding-value#sync" } %>
            </div>

            <div class="d-flex justify-content-end gap-2">
              <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
              <button type="submit" class="btn btn-success">Save to Portfolio</button>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% end %>